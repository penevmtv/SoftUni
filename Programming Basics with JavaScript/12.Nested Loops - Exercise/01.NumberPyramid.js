function solve(number) {
  for (let rows = 1; rows <= number; rows++) {
    let printRow = ` `;
    let currentNum = 0;
    for (let place = 1; ;place++) {
      printRow += `${currentNum} `;
      if (place === rows) {
        
      }
    }
  }
}
